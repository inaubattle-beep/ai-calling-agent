; ====================================================================
; Asterisk REST Interface (ARI) Configuration
; ====================================================================

[general]
enabled = yes
pretty = yes
allowed_origins = *

[ai-agent]
type = user
read_only = no
password = changeme_ari_secret
password_format = plain
